package com.hodzilla51.minesier.net;

/** A short-lived, client-only visual action performed by a turtle. */
public enum TurtleVisualAction {
  DIG,
  PLACE,
  PICKUP,
  OUT_OF_FUEL,
  ERROR,
  NET_SEND,
  NET_RECEIVE,
  NET_FORWARD
}
